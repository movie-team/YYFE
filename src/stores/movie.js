import { ref, computed } from 'vue'
import { defineStore } from 'pinia'

export const useCounterStore = defineStore('movie', () => {
  const signup = () => {
    
  }

  return { count, doubleCount, increment }
})
